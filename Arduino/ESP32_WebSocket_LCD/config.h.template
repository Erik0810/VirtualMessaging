// WiFi credentials
const char* ssid = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// WebSocket server settings
// For local testing, use your computer's IP address
// Windows: 'ipconfig' and look for IPv4 Address
// Mac/Linux: 'ifconfig' or 'ip addr' and look for inet
const char* websocket_server = "192.168.1.XXX";  // Your computer's IP
const int websocket_port = 5000;